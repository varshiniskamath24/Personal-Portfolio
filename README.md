# Varshini S Kamath - Portfolio

A dark, futuristic personal portfolio built with React.

## 🚀 Deploy to Vercel (Recommended — Free, 30 seconds)

1. Push this folder to a GitHub repo
2. Go to [vercel.com](https://vercel.com) → New Project → Import your repo
3. Click **Deploy** — done! Vercel auto-detects Create React App.

**Or via Vercel CLI:**
```bash
npm i -g vercel
vercel
```

## 🌐 Deploy to Netlify (Alternative)

1. Push to GitHub
2. Go to [netlify.com](https://netlify.com) → Add new site → Import from Git
3. Build command: `npm run build`
4. Publish directory: `build`
5. Click Deploy

## 💻 Run Locally

```bash
npm install
npm start
```

Opens at http://localhost:3000

## 🏗️ Build for Production

```bash
npm run build
```

Outputs to `/build` — upload this folder anywhere (Netlify Drop, GitHub Pages, etc.)

## ✏️ Customize

- **Your info**: Edit files in `src/sections/`
  - `Hero.js` — name, bio, stats
  - `About.js` — about text, education
  - `Experience.js` — work experience
  - `Projects.js` — projects list
  - `Skills.js` — tech stack + certifications
  - `Contact.js` — contact links

- **Colors & fonts**: Edit `src/index.css` → `:root` variables
- **Your LinkedIn URL**: Search & replace `varshini-sripathi-kamath` with your actual LinkedIn slug

## 🎨 Design

- Dark futuristic theme with purple/cyan accents
- DM Serif Display + Syne + DM Mono typography
- Custom animated cursor
- Animated code card, floating badges
- Typewriter role animation
- Smooth scroll, hover effects, scanline animation
- Fully responsive (mobile-first)
